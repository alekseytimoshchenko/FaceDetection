package com.example.facedetection.ui.generalPhotosScreen.adapters

import android.support.v7.widget.RecyclerView
import com.example.facedetection.data.local.model.ImageObj
import com.example.facedetection.ui.base.IAdapter
import android.support.v7.util.DiffUtil


class GeneralPhotoAdapter : IAdapter<List<ImageObj>>, RecyclerView.Adapter<GeneralPhotoAdapter.ViewHolder>() {
    override fun setContent(content: List<ImageObj>) {
//        MyDiffUtil<HomeScreenAdapterItem> diff = new MyDiffUtil<>(mHomeItems, iData);
//        DiffUtil.DiffResult result = DiffUtil . calculateDiff (diff, false);
//        mHomeItems = iData;
//        result.dispatchUpdatesTo(this);
    }


}